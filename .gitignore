.env
.env.prod
.venv/
__pycache__/
*.pyc
.DS_Store
.rclone/
.sync_state.json
*.key

# Ancien nom (au cas où un clone a encore le dossier)
data/

# Vault privé (sync OVH) — seul .gitkeep est versionné
vault/*
!vault/.gitkeep
.auth-password-local
