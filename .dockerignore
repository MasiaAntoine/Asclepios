app/node_modules
app/dist
app/.vite
**/.DS_Store
**/*.log
requirements.txt
# Never send clinical data into the Docker build context
data/
vault/
.env
# .env.prod is mounted at runtime, not baked into the image
